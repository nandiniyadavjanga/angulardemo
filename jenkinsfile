pipeline { 
    agent {
      docker {
        image '
    stages {
        stage ('Checkout') {
            steps {
                sh 'git checkout master'
            }
        }
        stage('Install Packages') {
      steps {
        sh 'npm install'
      }
    }
     stage ('Build') {
            steps {
                sh 'ng build'
            }
        }
   
    }
}
